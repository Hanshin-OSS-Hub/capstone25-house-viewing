<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.HouseViewingApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="popupMenuStyle">@style/PopupMenuStyle</item>
    </style>

    <style name="Theme.HouseViewingApp" parent="Base.Theme.HouseViewingApp">
    </style>

    <!-- PopupMenu 전용: ContextThemeWrapper로 넘겨서 배경 적용 -->
    <style name="Theme.PopupMenuOverlay" parent="Theme.HouseViewingApp">
        <item name="popupMenuStyle">@style/PopupMenuStyle</item>
    </style>

    <style name="PopupMenuStyle" parent="@android:style/Widget.Material.Light.PopupMenu">
        <item name="android:popupBackground">@drawable/bg_popup_menu</item>
        <item name="android:itemBackground">@android:color/transparent</item>
    </style>

</resources>
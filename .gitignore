# ── API 키 / 환경변수 (절대 올리면 안 됨) ──
.env

# ── Python 자동 생성 파일 ──
__pycache__/
*.pyc
*.pyo
*.pyd

# ── 가상환경 ──
venv/
.venv/
env/

# ── 테스트용 임시 파일 ──
*.pdf
test_*.json

# ── IDE 설정 ──
.vscode/
.idea/

# ── AI 도구 설정 (올리면 안 됨) ──
.claude/
